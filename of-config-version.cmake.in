# Cmake package version check file, auto-generated during build
set(PACKAGE_VERSION @OF_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @OF_VERSION_MAJOR@ AND
   "${PACKAGE_FIND_VERSION_MINOR}" EQUAL @OF_VERSION_MINOR@ AND
   "${PACKAGE_FIND_VERSION_PATCH}" EQUAL @OF_VERSION_PATCH@)

    set(PACKAGE_VERSION_COMPATIBLE 1)
    set(PACKAGE_VERSION_EXACT 1)

endif()
